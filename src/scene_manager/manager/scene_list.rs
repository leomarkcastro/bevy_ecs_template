// Enum that will be used as a global state for the game
#[derive(Clone, Eq, PartialEq, Debug, Hash)]
pub enum GameScene {
    Boracay,
    Scene01,
    Scene02,
    Scene03,
    Scene04,
    Scene05,
    Scene06,
}
